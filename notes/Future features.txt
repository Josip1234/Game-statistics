 

One game can be played on many platforms so we need more to more 
relation (another table which connects more platforms for one game),
same as multiple tables for genres.


 we need to connect profile with statistics for game and sequel.
 if game has multiple profiles, we need to connect that profile to 
 that game statistic. 

since one sequel can have many files we can say that one file can have only statistic for that file 
if there is another statistic for new game another file can be created for that sequel more data will 
have more files.

