in future versions if game need to have multiple genres, 
we need additional table which will have id from genre and 
id from game to have M:M connection 
for now, if game has multiple genres, 
we can mark it as mixed or something like that  

need to check create form need to put old values for fields to remember 
insert data if any of the errors occured. 

One game can be played on many platforms so we need more to more 
relation (another table which connects more platforms for one game),
same as multiple tables for genres.